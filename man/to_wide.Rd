% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{to_wide}
\alias{to_wide}
\title{Fast long-to-wide conversion with item selection}
\usage{
to_wide(df, keys, item_col, value_col, items = NULL, agg_fun = get_valid)
}
\arguments{
\item{df}{A data frame in long format.}

\item{keys}{A character vector of key column names.}

\item{item_col}{A single column name that contains item names.}

\item{value_col}{A single column name that contains values to spread.}

\item{items}{Optional character vector of items to keep in wide format.
If provided, output item columns follow this order and missing items are
kept as \code{NA} columns.}

\item{agg_fun}{Aggregation function used when key-item combinations have
multiple values. Default is \code{get_valid()}.}
}
\value{
A wide-format data frame.
}
\description{
Convert long-format data to wide format by grouping keys,
using one column as item names and one column as values. When there are
multiple values under the same key-item combination, values are reduced
by \code{agg_fun}. Designed to convert long-format clinical data in database
to wide format for analysis and publication.
}
\examples{
df <- data.frame(
  id = c(1, 1, 1, 2, 2),
  visit = c("v1", "v1", "v1", "v1", "v1"),
  item = c("A", "A", "B", "A", "C"),
  value = c(3, 5, 2, 1, 9)
)

to_wide(
  df,
  keys = c("id", "visit"),
  item_col = "item",
  value_col = "value",
  items = c("A", "B", "C"),
  agg_fun = max
)
}

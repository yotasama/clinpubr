% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{merge_by_range}
\alias{merge_by_range}
\title{Merge Data Frames by Exact Keys and Value Range}
\usage{
merge_by_range(
  x,
  y,
  by,
  x_start,
  x_end = NULL,
  y_val,
  range_relax = c(0, 0),
  all_y = TRUE,
  suffixes = c(".x", ".y")
)

merge_by_range(
  x,
  y,
  by,
  x_start,
  x_end = NULL,
  y_val,
  range_relax = c(0, 0),
  all_y = TRUE,
  suffixes = c(".x", ".y")
)
}
\arguments{
\item{x}{A data frame containing the range columns.}

\item{y}{A data frame containing the point-in-time value column.}

\item{by}{Either a character vector of column names that must match exactly
in both data frames, or a named list with elements \code{x} and \code{y} specifying
different column names in each data frame (e.g., \code{list(x = c("id1", "id2"), y = c("ID1", "ID2"))}).
The two vectors must have the same length and are matched by position.
Use \code{character(0)} when no exact-match keys are needed.}

\item{x_start}{Column name in \code{x} containing the range start.}

\item{x_end}{Column name in \code{x} containing the range end. If \code{NULL}, defaults
to \code{x_start} (treating the range as a single point).}

\item{y_val}{Column name in \code{y} containing the value to be matched.}

\item{range_relax}{A numeric vector of length 2 specifying how to extend the
matching range. The first element extends backwards from \code{x_start},
the second extends forwards from \code{x_end}. Default is \code{c(0, 0)} (no extension).
Both values must be >= 0.}

\item{all_y}{Logical, whether to keep rows from \code{y} that have no match.}

\item{suffixes}{Character vector of length 2 used for duplicated non-key
column names from \code{x} and \code{y}.}
}
\value{
A data frame containing matched rows from \code{x} and \code{y}. The output
includes a \code{since_start} column indicating the numeric difference between
\code{y_val} and \code{x_start} (in the units of the values, e.g., days for Date objects).

A data frame containing matched rows from \code{x} and \code{y}. The output
includes a \code{since_start} column indicating the numeric difference between
\code{y_val} and \code{x_start} (in the units of the values, e.g., days for Date objects).
}
\description{
Merge two data frames where shared keys in \code{by} must match exactly and the
value in \code{y[[y_val]]} must fall within the range defined by
\code{x[[x_start]]} and \code{x[[x_end]]}.

This function is particularly useful for date-based matching scenarios,
where you need to match events (e.g., examinations, treatments) to
time intervals (e.g., hospital admissions, visits). While the function
accepts any ordered values (numeric, Date, POSIXt), date matching is
the primary use case.

This avoids constructing the full Cartesian product that would be produced by
a regular equality join followed by range filtering.

Merge two data frames where shared keys in \code{by} must match exactly and the
value in \code{y[[y_val]]} must fall within the range defined by
\code{x[[x_start]]} and \code{x[[x_end]]}.

This function is particularly useful for date-based matching scenarios,
where you need to match events (e.g., examinations, treatments) to
time intervals (e.g., hospital admissions, visits). While the function
accepts any ordered values (numeric, Date, POSIXt), date matching is
the primary use case.

This avoids constructing the full Cartesian product that would be produced by
a regular equality join followed by range filtering.
}
\details{
Matching proceeds in three stages:
\enumerate{
\item Rows in \code{x} and \code{y} are first grouped by the exact-match keys in \code{by}.
\item Within each group, \code{y[[y_val]]} is matched against the interval defined by
\code{x[[x_start]]} and \code{x[[x_end]]}, optionally extended by \code{range_relax}.
\item When \code{range_relax} is non-zero, the relaxed intervals are clipped against
neighboring core intervals before matching, but never clipped further than
the original core interval.
}

If any row from \code{y} still matches multiple clipped ranges in \code{x}, a warning
is issued and \code{.cp_y_row_id} is retained in the output so duplicate matches
can be identified downstream.

When \code{all_y = TRUE}, rows from \code{y} with no match are appended to the result
with \code{NA} values for columns coming from \code{x} and for \code{since_start}.

Matching proceeds in three stages:
\enumerate{
\item Rows in \code{x} and \code{y} are first grouped by the exact-match keys in \code{by}.
\item Within each group, \code{y[[y_val]]} is matched against the interval defined by
\code{x[[x_start]]} and \code{x[[x_end]]}, optionally extended by \code{range_relax}.
\item When \code{range_relax} is non-zero, the relaxed intervals are clipped against
neighboring core intervals before matching, but never clipped further than
the original core interval.
}

If any row from \code{y} still matches multiple clipped ranges in \code{x}, a warning
is issued and \code{.cp_y_row_id} is retained in the output so duplicate matches
can be identified downstream.

When \code{all_y = TRUE}, rows from \code{y} with no match are appended to the result
with \code{NA} values for columns coming from \code{x} and for \code{since_start}.
}
\examples{
admissions <- data.frame(
  patient_id = c(1, 1, 2),
  date_start = as.Date(c("2024-01-01", "2024-02-01", "2024-03-01")),
  date_end = as.Date(c("2024-01-10", "2024-02-10", "2024-03-05")),
  ward = c("A", "B", "C")
)
examinations <- data.frame(
  patient_id = c(1, 1, 2, 3),
  exam_date = as.Date(c("2024-01-05", "2024-02-10", "2024-03-07", "2024-01-01")),
  exam_name = c("CT", "MRI", "XR", "US")
)

merge_by_range(
  x = admissions,
  y = examinations,
  by = "patient_id",
  x_start = "date_start",
  x_end = "date_end",
  y_val = "exam_date"
)

admissions <- data.frame(
  patient_id = c(1, 1, 2),
  date_start = as.Date(c("2024-01-01", "2024-02-01", "2024-03-01")),
  date_end = as.Date(c("2024-01-10", "2024-02-10", "2024-03-05")),
  ward = c("A", "B", "C")
)
examinations <- data.frame(
  patient_id = c(1, 1, 2, 3),
  exam_date = as.Date(c("2024-01-05", "2024-02-10", "2024-03-07", "2024-01-01")),
  exam_name = c("CT", "MRI", "XR", "US")
)

merge_by_range(
  x = admissions,
  y = examinations,
  by = "patient_id",
  x_start = "date_start",
  x_end = "date_end",
  y_val = "exam_date"
)

}

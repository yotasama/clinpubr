% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{merge_by_date_range}
\alias{merge_by_date_range}
\title{Merge Data Frames by Exact Keys and Date Range}
\usage{
merge_by_date_range(
  x,
  y,
  by,
  x_start,
  x_end,
  y_date,
  include_start = TRUE,
  include_end = TRUE,
  all.x = FALSE,
  all.y = FALSE,
  suffixes = c(".x", ".y"),
  engine = c("auto", "data.table", "base")
)
}
\arguments{
\item{x}{A data frame containing the date range columns.}

\item{y}{A data frame containing the point-in-time date column.}

\item{by}{Character vector of column names that must match exactly in both
data frames. Use \code{character(0)} when no exact-match keys are needed.}

\item{x_start}{Column name in \code{x} containing the range start date.}

\item{x_end}{Column name in \code{x} containing the range end date.}

\item{y_date}{Column name in \code{y} containing the date to be matched.}

\item{include_start}{Logical, whether the lower bound is inclusive.}

\item{include_end}{Logical, whether the upper bound is inclusive.}

\item{all.x}{Logical, whether to keep rows from \code{x} that have no match.}

\item{all.y}{Logical, whether to keep rows from \code{y} that have no match.}

\item{suffixes}{Character vector of length 2 used for duplicated non-key
column names from \code{x} and \code{y}.}

\item{engine}{Join engine. Use \code{"auto"} to prefer \code{data.table} when
installed and otherwise fall back to the base R implementation. Use
\code{"data.table"} to require the fast non-equi join path explicitly.}
}
\value{
A data frame containing matched rows from \code{x} and \code{y}. Rows may be
duplicated when one date matches multiple ranges or one range contains
multiple dates.
}
\description{
Merge two data frames where shared keys in \code{by} must match exactly and the
date in \code{y[[y_date]]} must fall within the date range defined by
\code{x[[x_start]]} and \code{x[[x_end]]}.

This avoids constructing the full Cartesian product that would be produced by
a regular equality join followed by date-range filtering.
}
\examples{
admissions <- data.frame(
  patient_id = c(1, 1, 2),
  date_start = as.Date(c("2024-01-01", "2024-02-01", "2024-03-01")),
  date_end = as.Date(c("2024-01-10", "2024-02-10", "2024-03-05")),
  ward = c("A", "B", "C")
)
examinations <- data.frame(
  patient_id = c(1, 1, 2, 3),
  exam_date = as.Date(c("2024-01-05", "2024-02-10", "2024-03-07", "2024-01-01")),
  exam_name = c("CT", "MRI", "XR", "US")
)

merge_by_date_range(
  x = admissions,
  y = examinations,
  by = "patient_id",
  x_start = "date_start",
  x_end = "date_end",
  y_date = "exam_date"
)

}
